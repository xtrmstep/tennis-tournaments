[project]
name = "competition-reg"
version = "0.1.0"
description = "Pair tennis tournament organizer CLI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0", "flask>=3.0", "flask-sqlalchemy>=3.1"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
